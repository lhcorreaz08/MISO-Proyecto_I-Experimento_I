version: '1.0'
services:
  appoffer:
    build:
      context: ./
      dockerfile: Dockerfile
    ports:
      - 3000:3000
    env_file:
      - ./.env
    depends_on:
      - dboffer
  dboffer:
    image: postgres:13-alpine
    env_file:
      - ./.env.db